# Phase 1 placeholder so Cube boots. Real cubes (suppliers, contracts, clauses,
# consents) and the supplier_risk_view land in Phase 2 (spec §5.1).
cubes:
  - name: phase1_placeholder
    sql: "SELECT 1 AS id"
    measures:
      - name: count
        type: count
    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true

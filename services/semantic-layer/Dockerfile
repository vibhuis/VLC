# Cube.dev semantic layer over the synthetic data via embedded DuckDB. [spec §5.1, DECISIONS D3]
FROM cubejs/cube:v1.1.7

# Cube reads its data model from /cube/conf/model.
COPY model /cube/conf/model
